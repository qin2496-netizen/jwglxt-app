# ---------------------------------------------------------------------------
# 依赖
# ---------------------------------------------------------------------------
node_modules/

# ---------------------------------------------------------------------------
# 构建产物（不需要进版本库，CI 会重新生成）
# ---------------------------------------------------------------------------
dist-apk/
android/app/build/
android/build/
android/.gradle/
android/.kotlin/
build/
ios/App/build/
ios/App/Pods/
ios/App/*.xcworkspace/xcuserdata/
ios/App/DerivedData/

# ---------------------------------------------------------------------------
# 本地环境相关（每台机器路径不同，不该共享）
# ---------------------------------------------------------------------------
.npm-cache/
.gradle-home/
android/local.properties
android/.idea/
*.iml

# ---------------------------------------------------------------------------
# 临时文件
# ---------------------------------------------------------------------------
*.log
x

# ---------------------------------------------------------------------------
# 签名密钥 —— 默认忽略，避免误推到公开仓库
# ---------------------------------------------------------------------------
# 安卓规定：同一 App 的后续版本必须用同一密钥签名，密钥丢失就只能卸载重装，
# 所以密钥**必须单独备份**到安全的地方（不是这里）。
#
# 若你确实想把密钥放进私有仓库，把下面两行注释掉，
# 并确保仓库是 Private、绝不变为 Public。
android/keystore/
android/keystore.properties

# iOS 签名相关（若以后配置了真机签名）
*.p12
*.mobileprovision
ios/App/ExportOptions.plist
