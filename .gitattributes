# ---------------------------------------------------------------------------
# 固定换行符，避免 Windows 与 Linux/macOS 之间互相改写
#
# 为什么必须配：
#   Git 默认在 Windows 上检出时把 LF 换成 CRLF（core.autocrlf）。
#   但 android/gradlew 是 **shell 脚本**，一旦被写成 CRLF，
#   在 Linux/macOS（CI 环境）里执行会报：
#       /bin/sh^M: bad interpreter: No such file or directory
#   同理 gradlew.bat 必须是 CRLF 才能在 Windows 上正常跑。
#   所以这里按文件类型强制指定，不依赖各人本地的 autocrlf 设置。
# ---------------------------------------------------------------------------

# 默认：仓库内一律 LF，检出时不做转换
* text=auto eol=lf

# Windows 批处理必须是 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Shell 脚本必须是 LF（CI 上要执行）
*.sh text eol=lf
gradlew text eol=lf

# 二进制文件不做任何换行符处理
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.jar binary
*.jks binary
*.keystore binary
*.wasm binary
*.zip binary
*.apk binary
*.aab binary
*.ipa binary
